# 1 JOIN

# 2 JOIN

# 3 JOIN

# 4 JOIN
No movies.
There is no join movement on the graph. eEther the join doesnt move,
or the electronics that process the movement has not worked.

# 5 JOIN
Movement not from the starting position.
Looks like the join hasnt returned to its original position.

# 6 JOIN